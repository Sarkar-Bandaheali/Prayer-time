<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Prayer Times</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
    <!-- Custom CSS -->
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <!-- Hero Section -->
    <div class="hero-section">
        <div class="container">
            <h1 class="text-center mb-4">Prayer Times</h1>
            <form action="/get-prayer-times" method="POST" class="mb-4">
                <div class="row g-3 justify-content-center">
                    <div class="col-md-6">
                        <input type="text" class="form-control" name="city" placeholder="Enter City (e.g., Nawabshah)" required>
                    </div>
                    <div class="col-md-2">
                        <button type="submit" class="btn btn-vip w-100">Get Prayer Times</button>
                    </div>
                </div>
            </form>
        </div>
    </div>

    <!-- Prayer Times Section -->
    <div class="container prayer-times-section">
        <% if (prayerTimes) { %>
            <div class="card vip-card">
                <div class="card-header">
                    <h2 class="card-title text-center">Prayer Times in <%= city %></h2>
                </div>
                <div class="card-body">
                    <ul class="list-group list-group-flush">
                        <li class="list-group-item"><strong>Fajr:</strong> <%= prayerTimes.fajr %></li>
                        <li class="list-group-item"><strong>Shurooq:</strong> <%= prayerTimes.shurooq %></li>
                        <li class="list-group-item"><strong>Dhuhr:</strong> <%= prayerTimes.dhuhr %></li>
                        <li class="list-group-item"><strong>Asr:</strong> <%= prayerTimes.asr %></li>
                        <li class="list-group-item"><strong>Maghrib:</strong> <%= prayerTimes.maghrib %></li>
                        <li class="list-group-item"><strong>Isha:</strong> <%= prayerTimes.isha %></li>
                    </ul>
                </div>
            </div>
        <% } else { %>
            <div class="text-center mt-4">
                <p>No prayer times available. Please check the city name and try again.</p>
            </div>
        <% } %>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
